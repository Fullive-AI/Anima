{
  "name": "anima",
  "version": "0.1.0",
  "private": true,
  "description": "Make Every Hardware Intelligent",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:frontend": "pnpm --filter dashboard dev",
    "dev:backend": ".venv/bin/python -m core.main",
    "dev:broker": ".venv/bin/amqtt -c amqtt.yaml",
    "build": "pnpm --filter dashboard build",
    "postinstall": "uv sync --extra dev --python 3.13"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "playwright": "^1.59.1"
  }
}
